FROM node:16

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

ARG DATABASE_URL
ARG BROKER_SERVER

ENV DATABASE_URL=${DATABASE_URL}
ENV BROKER_SERVER=${BROKER_SERVER}
ENV PORT="8080"
ENV PATH="/usr/bin:$PATH" 

EXPOSE 8080

CMD ["npm", "run", "deploy"]
